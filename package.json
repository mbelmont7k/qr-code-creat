{
  "name": "qr-admin-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Single-admin dynamic QR management platform.",
  "engines": { "node": ">=22.5" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "create-admin": "node src/create-admin.js",
    "test": "node --test tests/**/*.test.js",
    "audit:deps": "npm audit --omit=dev"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "qrcode": "^1.5.4",
    "sharp": "^0.35.4"
  }
}
